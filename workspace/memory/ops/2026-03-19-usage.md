# Daily Usage Observability Report — 2026-03-19

Generated: 2026-03-19 22:55 UTC

---

## 1. Usage Cost Snapshot

| Period | Tokens | Cost |
|--------|--------|------|
| Last 30 days | 180.1M | $129.64 |
| Today (2026-03-19) | 10.7M | $8.72 |

---

## 2. Cron Health Summary

| Metric | Value |
|--------|-------|
| Total jobs | 14 |
| Enabled jobs | 12 |
| Disabled jobs | 2 |
| Jobs with errors (consecutiveErrors > 0) | 4 |
| Jobs at high risk (consecutiveErrors ≥ 3) | 0 |

---

## 3. Jobs At Risk

| Job | consecutiveErrors | Last Error |
|-----|------------------|------------|
| cron-circuit-breaker | 2 | rate_limit (OAuth + API rate limits) |
| heartbeat-klajdi | 2 | rate_limit (OAuth + API rate limits) |
| gateway-watchdog | 1 | rate_limit (OAuth + API rate limits) |
| maintenance-batch | 1 | rate_limit (OAuth + API rate limits) |

All 4 failing jobs share the same root cause: `openai-codex` OAuth token refresh failure combined with Anthropic API rate limits exhausting all model fallbacks.

---

## 4. Recommended Actions

- **Re-authenticate openai-codex OAuth token** — all 4 failing jobs fail at the codex model first; fixing auth will restore primary model availability and reduce fallback pressure.
- **Stagger high-frequency jobs** — heartbeat-klajdi (every 3h) and maintenance-batch (every 6h) fire close together and both hit rate limits; add buffer or reduce frequency temporarily.
- **Monitor cron-circuit-breaker** — it has 2 consecutive errors; if it hits 3, it may self-disable and stop protecting other jobs from cascading failures.
