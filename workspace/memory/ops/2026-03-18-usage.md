# Daily Usage Observability Report — 2026-03-18

## 1. Usage Cost Snapshot
- 30-day total cost: **$121.51**
- 30-day total tokens: **170.3m**
- Latest day (2026-03-18): **$0.58** / **864k tokens**
- Doctor warning observed: Telegram `groupPolicy` is `allowlist` with empty allowlist (`groupAllowFrom`/`allowFrom`).

## 2. Cron Health Summary
- Total jobs: **12**
- Enabled jobs: **12**
- Failing jobs (`consecutiveErrors > 0`): **0**
- High-risk jobs (`consecutiveErrors >= 3`): **0**

## 3. Jobs At Risk
- None (0 jobs with `consecutiveErrors > 0`).

## 4. Recommended Actions
- Keep cron schedules unchanged; reliability is currently healthy.
- Resolve Telegram allowlist warning by adding sender IDs to `channels.telegram.groupAllowFrom`/`allowFrom`, or set `groupPolicy` to `open` if intended.
- Continue daily observability runs to detect regressions early.
