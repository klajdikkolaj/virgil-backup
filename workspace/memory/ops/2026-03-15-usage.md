# Daily Usage Observability Report — 2026-03-15

## 1. Usage Cost Snapshot
- 30-day total cost: **$118.96**
- 30-day total tokens: **165.9m**
- Latest day (2026-03-15): **$1.33** / **2.1m tokens**

## 2. Cron Health Summary
- Total jobs: **12**
- Enabled jobs: **12**
- Failing jobs (`consecutiveErrors > 0`): **0**
- High-risk jobs (`consecutiveErrors >= 3`): **0**

## 3. Jobs At Risk
- None (0 jobs with `consecutiveErrors > 0`).

## 4. Recommended Actions
- Keep current cron schedules unchanged; reliability is healthy.
- Resolve Telegram doctor warning by populating `channels.telegram.groupAllowFrom`/`allowFrom` or setting `groupPolicy` to `open` if intended.
- Continue daily usage observability runs to catch early regressions.
