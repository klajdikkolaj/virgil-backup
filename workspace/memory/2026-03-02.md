# 2026-03-02

- Klajdi preference: only notify about model changes when they are automatic fallback events (not for manual model switches).

## Agent Orchestration Changes Applied (2026-03-02 ~13:30–14:00 UTC)

### Config changes made
- Model fallback order: Sonnet (primary) → Codex → Opus
- `thinkingDefault = medium` (global — affects Sonnet + Codex fallback; subagents still use `thinking: low`)
- Subagent defaults: model=Codex, thinking=low, runTimeoutSeconds=900
- ACP enabled: `acp.enabled=true`, dispatch enabled, backend=acpx, defaultAgent=codex, allowedAgents=[claude, codex]
- Session thread bindings enabled
- Model aliases confirmed: sonnet / opus / codex

### Cron cleanup
- Removed duplicate morning briefing job (55ccbc9f-427d-460d-a9df-96ca664db369)
- Updated heartbeat delivery → Telegram, chat_id 2016260249

### New workflow docs created
- `memory/workflows/channel-routing.md` — routing policy (Sonnet/Opus/Codex lanes, escalation triggers, safety gates)
- `memory/workflows/knowledge-access-order.md` — enforces: memory recall → Obsidian/QMD check → synthesize before answering project/strategy questions

### Backup
- Saved to: `backups/agent-orchestration-2026-03-02-133104/`

### Klajdi preferences confirmed today
- Notify on automatic fallback only (not manual model switches)
- Fallback Codex should use medium thinking (Sonnet-comparable)
- gog auth not yet set up (no Gmail access until OAuth completed)

### Codex thinking escalation policy (confirmed with Klajdi)
- Default: medium (thinkingDefault in config)
- High: security-critical review, complex debugging, ambiguous multi-system tradeoffs, Eternia governance/alignment decisions
- Extra-high: deep security audits, complex cascading refactors, high-stakes decisions, when high already failed
- Always notify Klajdi when escalating thinking level and state reason

### Implementation checklist gap analysis (as of 14:38 UTC)
**Done:** Phase 0 (backup), Phase 1 (model catalog/aliases/fallback), Phase 2 partial (channel-routing.md created), Phase 3 (subagent defaults), Phase 4 (ACP), Phase 5 (cron cleanup)

**Still missing:**
- Phase 2 gap: `#research` channel NOT actually routed to Opus yet — no auto-routing mechanism in place. Klajdi may have something set up already (to check)
- `threadBindings.spawnAcpSessions = true` — NOT applied yet (was in checklist)
- Phase 6 (policy/privacy hardening) — implicitly enforced but not formally audited
- Phase 7 (validation tests) — none run yet
- Phase 8 (7-day evaluation tracking) — not started

**Next actions to confirm with Klajdi:**
1. Check `#research` channel config / what Klajdi set up there
2. Apply `threadBindings.spawnAcpSessions = true` if desired
3. Fix opencortex-memory-review cron `delivery.to` for Telegram
4. Fix doctor warning: telegram groupPolicy allowlist empty

### Per-channel model routing applied (14:45 UTC)
- Klajdi provided explicit channel routing map.
- Applied with `agents.list` + `bindings`:
  - `1477637773681885184` → `virgil-opus` (Opus)
  - `1477637814014316586` → `virgil-haiku` (Haiku)
  - `1477637736964952075`, `1477637792157794369`, `1477637827964571780`, `1477606618224132274` → `main` (Sonnet default)
- Added channel `1477606618224132274` to Discord guild channel allowlist.
- Created agent definitions:
  - `main` (Sonnet), `virgil-opus` (Opus), `virgil-haiku` (Haiku)
- Fallback behavior for routed agents:
  - Opus agent fallback → Sonnet
  - Haiku agent fallback → Sonnet

### Checklist status update after routing
- Previous Phase 2 routing gap is now resolved (research channel routed to Opus).
- Remaining gaps still open: `threadBindings.spawnAcpSessions=true`, formal validation tests, 7-day evaluation tracking.

### Known open issues
- opencortex-memory-review cron missing `delivery.to` for Telegram
- Doctor warning: `channels.telegram.groupPolicy` allowlist has empty allowed sender list for groups

### Fallback routing update (15:05 UTC)
- Updated agent fallback chains:
  - `virgil-opus`: Opus → Codex
  - `main`: Sonnet → Codex → Opus
  - `virgil-haiku`: Haiku → Codex → Sonnet
- This replaced previous Opus/Haiku fallback-to-Sonnet-only behavior.

### Thinking-level constraint discovered
- Attempted to set per-agent `thinkingDefault` in `agents.list` (e.g., Opus lane xhigh, main medium, Haiku low).
- Config schema rejected it (`invalid config`) — per-agent `thinkingDefault` in `agents.list` is not supported in current config.
- Effective behavior remains global: `agents.defaults.thinkingDefault = medium`.
- Result: Codex fallback thinking is medium across lanes unless manually overridden per message.

### Klajdi intent (unfulfilled by native config)
- Requested: Opus lane fallback to Codex xhigh; main lane Codex medium; Haiku lane Codex low.
- Native config cannot set different Codex thinking by lane through fallback only.
- Workaround discussed: use manual `/think xhigh` in research turns when needed.

### Protocol controls rollout (16:11–16:17 UTC)
- Klajdi approved applying the anti-deviation controls beyond research.
- Created `memory/workflows/protocol-levels.md` with tiered controls:
  - STRICT / MEDIUM / LIGHT
  - fail-closed behavior
  - mandatory preflight checklist
  - no silent method changes
  - required synthesis order
  - completion contract + protocol check line
- Added channel mapping by risk:
  - `#research` STRICT
  - `#general`, `#inbox`, `#briefing`, `#monitoring` MEDIUM (STRICT on incidents/security)
  - `#automation` LIGHT unless side-effects then escalate
- Linked this protocol file from `memory/workflows/channel-routing.md` for discoverability.
- Offered next step: one-line protocol reminder at task start in each channel.

### Protocol hardening observation (16:11 UTC)
- Klajdi reported deviation in `#research` and corrected it with: fail-closed behavior, mandatory preflight checklist, no silent method changes, required synthesis order, completion contract, and protocol check line.
- Guidance given: apply these controls across all lanes with tiered strictness by risk:
  - strict for research/security/external actions
  - medium for general ops/monitoring
  - light for low-risk retrieval/Q&A
- Potential next artifact: `protocol-levels.md` mapped by channel/lane.

### Monetization thread (20:44–21:00 UTC)
- Klajdi asked how to make money with automation.
- Drafted Offer #1: "AI Follow-Up System Setup" (template-based multi-client delivery, no-code stack).
- Saved offer details to: `memory/ideas/monetization-offers.md`.
- Klajdi asked for another automation/tech/software-selling idea next.
