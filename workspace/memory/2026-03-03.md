# 2026-03-03

## QMD indexing optimization applied

- Added `workspace-memory` collection:
  - Path: `/home/clawdkbot/.openclaw/workspace/memory`
  - Pattern: `**/*.md`
- Added targeted contexts for vault subpaths:
  - `/research`, `/projects`, `/daily`, `/skills`
- Added operational context for `workspace-memory` root.
- Ran `qmd update` and `qmd embed` successfully.
- Post-change index status:
  - 26 files indexed
  - 49 vectors embedded
  - 2 collections (`vault`, `workspace-memory`)

## Scheduled optimization

- Added cron job `qmd-light-refresh` (every 3 hours, Europe/Tirane):
  - Runs lightweight `qmd update` between nightly full reindex cycles.
  - Delivery mode: none (silent maintenance).

## Remaining issue

- QMD still attempts CUDA build first and fails (`CUDA Toolkit not found`), then falls back to CPU.
- This is performance/log-noise debt; needs explicit CPU-only runtime config or full CUDA install.

## OpenCortex memory review (09:00 UTC cron)

- Reviewed recent daily logs: `2026-02-27.md`, `2026-03-01.md`, `2026-03-02.md`, `2026-03-03.md`.
- Distilled stable preferences and ops decisions into long-term memory.
- Updated `MEMORY.md`:
  - added model-notification preference (fallback-only alerts)
  - added Codex reasoning escalation policy
  - added QMD light-refresh cron and workspace-memory indexing status
  - added protocol/workflow pointers for routing and quality controls
  - refreshed known issues/debts
- Restructured long-term vault memory files for retention clarity:
  - `memory/contacts/klajdi.md` (communication + AI preferences)
  - `memory/preferences.md` (stable behavior rules)
  - `memory/projects/eternia.md` (project status + execution governance links)
- `memory_search` tool was unavailable during this run (embedding provider/auth error), so distillation was done via direct file reads.

## OpenCortex memory review refresh (09:09 UTC cron run)

- Re-read recent daily logs and reconciled stable vs volatile facts.
- Rewrote `MEMORY.md` into a cleaner long-term format:
  - stable profile of Klajdi
  - Eternia strategic sequencing rule + phase map
  - operating environment snapshot
  - critical workflow pointers
  - explicit open-loop verification debts
- Re-structured retention files for clarity and lower duplication:
  - `memory/contacts/klajdi.md`
  - `memory/projects/eternia.md`
  - `memory/preferences.md`
- Flagged one consistency debt for next validation: Gmail/Calendar auth state appears inconsistent across prior logs and should be explicitly re-verified before reliance.

## QMD CPU-only mode enabled (10:12 UTC)

- Klajdi requested clean CPU-only mode for QMD.
- Updated QMD runtime behavior to default CPU-only (no CUDA probing/build attempt) by patching:
  - `/home/linuxbrew/.linuxbrew/lib/node_modules/@tobilu/qmd/dist/llm.js`
  - logic now defaults to CPU unless explicitly disabled with `QMD_CPU_ONLY=0`.
- Added shell-level env defaults for consistency:
  - `~/.bashrc`: `export QMD_CPU_ONLY=1`, `export NODE_LLAMA_CPP_GPU=off`
  - `~/.profile`: same exports
- Updated QMD cron payloads to enforce CPU-only explicitly:
  - `vault-qmd-reindex`
  - `qmd-light-refresh`
- Validation: `qmd status` now runs without CUDA build failure spam and reports CPU mode cleanly.

## Monitoring and maintenance cron stabilization (March 3)

- Heartbeat visibility behavior confirmed:
  - `heartbeat-klajdi` runs on schedule and returns `HEARTBEAT_OK` when no issues are found.
  - `HEARTBEAT_OK` runs are intentionally non-delivered (silent), so no Discord message appears unless there is an alert.
- Delivery routing updates applied:
  - `heartbeat-klajdi` delivery now targets Discord `#monitoring` (`1477637814014316586`) instead of Telegram.
  - `virgil-haiku` heartbeat enabled at `1h` and targeted to Discord `#monitoring`.
- Investigated missed `daily-maintenance` behavior:
  - Cron did trigger at scheduled time (03:00 UTC / 04:00 Europe/Tirane), but the job restarted `openclaw-gateway.service` from inside its own run.
  - That self-restart terminated the running cron session before it could finalize run state, making it look skipped/idle.
- Durable fix applied to `daily-maintenance` job:
  - Removed in-job `systemctl --user restart openclaw-gateway.service` step.
  - Kept update/version/skills checks.
  - Added explicit instruction to avoid self-restart in the job payload.
  - Set delivery for maintenance summary to Discord `#monitoring` (`1477637814014316586`).
- Update outcome check:
  - OpenClaw update did partially advance repo state (behind count dropped from ~408 to ~49 at check time), confirming update activity occurred even with interrupted maintenance session accounting.
