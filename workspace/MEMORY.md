# MEMORY.md — Virgil Long-Term Memory

## Context Management Protocol
When context usage reaches ~70%, I must:
1. Write a session summary to `memory/YYYY-MM-DD.md` covering key decisions, preferences, and what was done
2. Update relevant memory files (projects, contacts, preferences) with anything new
3. Run `openclaw memory index` to reindex
4. Notify Klajdi: "Context is getting full — I've saved our session to memory. We can continue."

This applies to ALL channels (Telegram, Discord, etc.)
